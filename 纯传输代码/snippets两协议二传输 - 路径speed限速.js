import{connect}from'cloudflare:sockets';
const uuid = 'd342d11e-d424-4583-b36e-524ab1f0afa4';
//**警告**:trojan使用的sha224密钥，需要自己计算，当前设置为密码666的密钥
//**警告**:trojan使用的sha224密钥，需要自己计算，当前设置为密码666的密钥
//**警告**:trojan使用的sha224密钥，需要自己计算，当前设置为密码666的密钥
//**警告**:trojan使用的sha224密钥计算网址：https://www.lzltool.com/data-sha224
const passWordSha224 = '509eece82eb6910bebef9af9496092d3244b6c0d69ef3aaa4b12c565';
// URL路径添加 speed=N 可限制下行速度，单位为 MB/s，例如 /?speed=50。
const bufferSize=256*1024;const startThreshold=50*1024*1024;const maxChunkLen=64*1024;const flushTime=4;const urlParamCacheLimit=20;const proxyStrategyOrder=["socks","http","https","turn","turns"];const dohEndpoint="https://cloudflare-dns.com/dns-query";const proxyIpAddrs={EU:"eu.proxy.58807.de5.net",AS:"sg.proxy.58807.de5.net",JP:"jp.proxy.58807.de5.net",US:"us.proxy.58807.de5.net"};const finallyProxyHost="proxy.58807.de5.net";const coloRegions={JP:new Set(["FUK","ICN","KIX","NRT","OKA"]),EU:new Set(["ACC","ADB","ALA","ALG","AMM","AMS","ARN","ATH","BAH","BCN","BEG","BGW","BOD","BRU","BTS","BUD","CAI","CDG","CPH","CPT","DAR","DKR","DMM","DOH","DUB","DUR","DUS","DXB","EBB","EDI","EVN","FCO","FRA","GOT","GVA","HAM","HEL","HRE","IST","JED","JIB","JNB","KBP","KEF","KWI","LAD","LED","LHR","LIS","LOS","LUX","LYS","MAD","MAN","MCT","MPM","MRS","MUC","MXP","NBO","OSL","OTP","PMO","PRG","RIX","RUH","RUN","SKG","SOF","STR","TBS","TLL","TLV","TUN","VIE","VNO","WAW","ZAG","ZRH"]),AS:new Set(["ADL","AKL","AMD","BKK","BLR","BNE","BOM","CBR","CCU","CEB","CGK","CMB","COK","DAC","DEL","HAN","HKG","HYD","ISB","JHB","JOG","KCH","KHH","KHI","KTM","KUL","LHE","MAA","MEL","MFM","MLE","MNL","NAG","NOU","PAT","PBH","PER","PNH","SGN","SIN","SYD","TPE","ULN","VTE"])};const coloToProxyMap=new Map;for(const[region,colos]of Object.entries(coloRegions)){for(const colo of colos)coloToProxyMap.set(colo,proxyIpAddrs[region])}const uuidBytes=new Uint8Array(16),hashBytes=new Uint8Array(56),offsets=[0,0,0,0,1,1,2,2,3,3,4,4,4,4,4,4];for(let i=0,c;i<16;i++)uuidBytes[i]=(((c=uuid.charCodeAt(i*2+offsets[i]))>64?c+9:c)&15)<<4|((c=uuid.charCodeAt(i*2+offsets[i]+1))>64?c+9:c)&15;for(let i=0;i<56;i++)hashBytes[i]=passWordSha224.charCodeAt(i);const[textEncoder,textDecoder,socks5Init]=[new TextEncoder,new TextDecoder,new Uint8Array([5,2,0,2])];const html=`<html><head><title>404 Not Found</title></head><body><center><h1>404 Not Found</h1></center><hr><center>nginx/1.25.3</center></body></html>`;const binaryAddrToString=(addrType,addrBytes)=>{if(addrType===3)return textDecoder.decode(addrBytes);if(addrType===1)return`${addrBytes[0]}.${addrBytes[1]}.${addrBytes[2]}.${addrBytes[3]}`;let ipv6=(addrBytes[0]<<8|addrBytes[1]).toString(16);for(let i=1;i<8;i++)ipv6+=":"+(addrBytes[i*2]<<8|addrBytes[i*2+1]).toString(16);return`[${ipv6}]`};const parseHostPort=(addr,defaultPort)=>{let host=addr,port=defaultPort,idx;if(addr.charCodeAt(0)===91){if((idx=addr.indexOf("]:"))!==-1){host=addr.substring(0,idx+1);port=addr.substring(idx+2)}}else if((idx=addr.indexOf(".tp"))!==-1&&addr.lastIndexOf(":")===-1){port=addr.substring(idx+3,addr.indexOf(".",idx+3))}else if((idx=addr.lastIndexOf(":"))!==-1){host=addr.substring(0,idx);port=addr.substring(idx+1)}return[host,(port=parseInt(port),isNaN(port)?defaultPort:port)]};const parseAuthString=authParam=>{let username,password,hostStr;const atIndex=authParam.lastIndexOf("@");if(atIndex===-1){hostStr=authParam}else{const cred=authParam.substring(0,atIndex);hostStr=authParam.substring(atIndex+1);const colonIndex=cred.indexOf(":");if(colonIndex===-1){username=cred}else{username=cred.substring(0,colonIndex);password=cred.substring(colonIndex+1)}}const[hostname,port]=parseHostPort(hostStr,1080);return{username:username,password:password,hostname:hostname,port:port}};const createConnect=(hostname,port,socketOptions,socket=connect({hostname:hostname,port:port},socketOptions))=>socket.opened.then(()=>socket);const connectViaSocksProxy=async(targetAddrType,targetPortNum,socksAuth,addrBytes)=>{const socksSocket=await createConnect(socksAuth.hostname,socksAuth.port);const writer=socksSocket.writable.getWriter();const reader=socksSocket.readable.getReader();await writer.write(socks5Init);const{value:authResponse}=await reader.read();if(!authResponse||authResponse[0]!==5||authResponse[1]===255)return null;if(authResponse[1]===2){if(!socksAuth.username)return null;const userBytes=textEncoder.encode(socksAuth.username);const passBytes=textEncoder.encode(socksAuth.password||"");const uLen=userBytes.length,pLen=passBytes.length,authReq=new Uint8Array(3+uLen+pLen);authReq[0]=1,authReq[1]=uLen,authReq.set(userBytes,2),authReq[2+uLen]=pLen,authReq.set(passBytes,3+uLen);await writer.write(authReq);const{value:authResult}=await reader.read();if(!authResult||authResult[0]!==1||authResult[1]!==0)return null}else if(authResponse[1]!==0){return null}const isDomain=targetAddrType===3,socksReq=new Uint8Array(6+addrBytes.length+(isDomain?1:0));socksReq[0]=5,socksReq[1]=1,socksReq[2]=0,socksReq[3]=targetAddrType;isDomain?(socksReq[4]=addrBytes.length,socksReq.set(addrBytes,5)):socksReq.set(addrBytes,4);socksReq[socksReq.length-2]=targetPortNum>>8,socksReq[socksReq.length-1]=targetPortNum&255;await writer.write(socksReq);const{value:finalResponse}=await reader.read();if(!finalResponse||finalResponse[1]!==0)return null;writer.releaseLock(),reader.releaseLock();return socksSocket};const staticHeaders=`User-Agent:Mozilla/5.0(X11;Linux x86_64)AppleWebKit/537.36\r\nProxy-Connection:Keep-Alive\r\nConnection:Keep-Alive\r\n\r\n`;const encodedStaticHeaders=textEncoder.encode(staticHeaders);const connectViaHttpProxy=async(targetAddrType,targetPortNum,httpAuth,addrBytes,useTls=false)=>{const{username:username,password:password,hostname:hostname,port:port}=httpAuth;const connectOptions=useTls?{secureTransport:"on",allowHalfOpen:false}:undefined;const proxySocket=await createConnect(hostname,port,connectOptions);const writer=proxySocket.writable.getWriter();const httpHost=binaryAddrToString(targetAddrType,addrBytes);let dynamicHeaders=`CONNECT ${httpHost}:${targetPortNum} HTTP/1.1\r\nHost:${httpHost}:${targetPortNum}\r\n`;if(username)dynamicHeaders+=`Proxy-Authorization:Basic ${btoa(`${username}:${password||""}`)}\r\n`;const fullHeaders=new Uint8Array(dynamicHeaders.length*3+encodedStaticHeaders.length);const{written:written}=textEncoder.encodeInto(dynamicHeaders,fullHeaders);fullHeaders.set(encodedStaticHeaders,written);await writer.write(fullHeaders.subarray(0,written+encodedStaticHeaders.length));writer.releaseLock();const reader=proxySocket.readable.getReader();const buffer=new Uint8Array(512);let bytesRead=0,statusChecked=false;while(bytesRead<buffer.length){const{value:value,done:done}=await reader.read();if(done||bytesRead+value.length>buffer.length)return null;const prevBytesRead=bytesRead;buffer.set(value,bytesRead);bytesRead+=value.length;if(!statusChecked&&bytesRead>=12){if(buffer[9]!==50)return null;statusChecked=true}let i=Math.max(15,prevBytesRead-3);while((i=buffer.indexOf(13,i))!==-1&&i<=bytesRead-4){if(buffer[i+1]===10&&buffer[i+2]===13&&buffer[i+3]===10){reader.releaseLock();return proxySocket}i++}}return null};const magic=new Uint8Array([33,18,164,66]);const cat=(...a)=>{let len=0,i=0,o=0;for(;i<a.length;i++)len+=a[i].length;const r=new Uint8Array(len);for(i=0;i<a.length;i++){r.set(a[i],o);o+=a[i].length}return r};const stunAttr=(t,v)=>{const l=v.length,b=new Uint8Array(4+l+(4-l%4)%4);b[0]=t>>8,b[1]=t&255,b[2]=l>>8,b[3]=l&255,b.set(v,4);return b};const stunMsg=(t,tid,a)=>{const bd=cat(...a),l=bd.length,h=new Uint8Array(20+l);h[0]=t>>8,h[1]=t&255,h[2]=l>>8,h[3]=l&255,h.set(magic,4),h.set(tid,8),h.set(bd,20);return h};const xorPeer=(ip,port)=>{const b=new Uint8Array(8);b[1]=1;const xp=port^8466;b[2]=xp>>8,b[3]=xp&255;let p=0,num=0;for(let i=0;i<ip.length;i++){const c=ip.charCodeAt(i);if(c===46){b[4+p]=num^magic[p++];num=0}else{num=num*10+(c-48)}}b[4+p]=num^magic[p];return b};const parseStun=d=>{if(d.length<20||magic.some((v,i)=>d[4+i]!==v))return null;const ml=d[2]<<8|d[3],attrs={};for(let o=20;o+4<=20+ml;){const t=d[o]<<8|d[o+1],l=d[o+2]<<8|d[o+3];if(o+4+l>d.length)break;attrs[t]=d.subarray(o+4,o+4+l);o+=4+l+(4-l%4)%4}return{type:d[0]<<8|d[1],attrs:attrs,tid:d.slice(8,20)}};const parseErr=d=>d?.length>=4?(d[2]&7)*100+d[3]:0;const addIntegrity=async(m,cryptoKey)=>{const l=m.length,c=new Uint8Array(l+24);c.set(m);const nl=(m[2]<<8|m[3])+24;c[2]=nl>>8,c[3]=nl&255;const sig=new Uint8Array(await crypto.subtle.sign("HMAC",cryptoKey,c.subarray(0,l)));c[l]=0,c[l+1]=8,c[l+2]=0,c[l+3]=20,c.set(sig,l+4);return c};const readStun=async(rd,buf)=>{let chunks=buf&&buf.length?[buf]:[];let total=buf?buf.length:0;const pull=async()=>{const{done:done,value:value}=await rd.read();if(done)throw 0;chunks.push(value);total+=value.length};const getB=()=>{if(chunks.length===1)return chunks[0];const b=new Uint8Array(total);let o=0;for(let i=0;i<chunks.length;i++){b.set(chunks[i],o);o+=chunks[i].length}chunks=[b];return b};try{while(total<20)await pull();let b=getB();if(b[4]!==33||b[5]!==18||b[6]!==164||b[7]!==66)return null;const n=20+(b[2]<<8|b[3]);if(n>8192)return null;while(total<n)await pull();b=getB();return[parseStun(b.subarray(0,n)),total>n?b.subarray(n):null]}catch{return null}};const md5=async s=>new Uint8Array(await crypto.subtle.digest("MD5",textEncoder.encode(s)));const connectViaTurnProxy=async({hostname:hostname,port:port,username:username,password:password},{addrType:addrType,port:targetPort,addrBytes:addrBytes},useTls=false)=>{let targetIp=binaryAddrToString(addrType,addrBytes);if(addrType===3){targetIp=dnsAResult(targetIp).catch(()=>null)}else if(addrType===4){return null}let ctrl=null,data=null,dataPromise=null;let cw=null,cr=null,ctrlExtra=null,closed=false;const close=()=>{closed=true;[ctrl,data].forEach(s=>{try{s?.close()}catch{}});[cr,cw].forEach(lock=>{try{lock?.releaseLock()}catch{}})};const createConn=()=>{const socketOptions=useTls?{secureTransport:"on",allowHalfOpen:false}:undefined;const socket=connect({hostname:hostname,port:port},socketOptions);return createConnect(hostname,port,socketOptions,socket).catch(e=>{try{socket.close()}catch{}throw e})};const newTid=()=>crypto.getRandomValues(new Uint8Array(12));const sameTid=(a,b)=>a?.length===b?.length&&a.every((v,i)=>v===b[i]);const tidKey=tid=>{let key="";for(let i=0;i<tid.length;i++)key+=tid[i].toString(16).padStart(2,"0");return key};const readMatching=async(rd,expectedTid,buffered=null,pending=null)=>{const expectedKey=tidKey(expectedTid),cached=pending?.get(expectedKey);if(cached){pending.delete(expectedKey);return[cached,buffered]}let extra=buffered;for(;;){const result=await readStun(rd,extra);if(!result)throw 0;const[msg,next]=result;extra=next;if(sameTid(msg.tid,expectedTid))return[msg,extra];if(pending)pending.set(tidKey(msg.tid),msg)}};const ctrlPending=new Map;const readControl=async expectedTid=>{const[msg,extra]=await readMatching(cr,expectedTid,ctrlExtra,ctrlPending);ctrlExtra=extra;return msg};let cryptoKey=null,aa=[];const sign=m=>cryptoKey?addIntegrity(m,cryptoKey):m;try{const ctrlPromise=createConn();dataPromise=createConn().then(socket=>{data=socket;if(closed)try{socket.close()}catch{}return socket});dataPromise.catch(()=>{});ctrl=await ctrlPromise;cw=ctrl.writable.getWriter(),cr=ctrl.readable.getReader();let tid=newTid();await cw.write(stunMsg(3,tid,[stunAttr(25,new Uint8Array([6,0,0,0]))]));let r=await readControl(tid);if(!r)throw 0;const targetAddress=await targetIp;if(!targetAddress)throw 0;const peer=stunAttr(18,xorPeer(targetAddress,targetPort));let permissionTid=null,connectTid=null,pm=null,cm=null;if(r.type===275&&username&&parseErr(r.attrs[9])===401){const realm=textDecoder.decode(r.attrs[20]??[]),nonce=r.attrs[21]??[];const keyBytes=await md5(`${username}:${realm}:${password}`);cryptoKey=await crypto.subtle.importKey("raw",keyBytes,{name:"HMAC",hash:"SHA-1"},false,["sign"]);aa=[stunAttr(6,textEncoder.encode(username)),stunAttr(20,textEncoder.encode(realm)),stunAttr(21,nonce)];const allocateTid=newTid();permissionTid=newTid(),connectTid=newTid();const[am,permissionMsg,connectMsg]=await Promise.all([sign(stunMsg(3,allocateTid,[stunAttr(25,new Uint8Array([6,0,0,0])),...aa])),sign(stunMsg(8,permissionTid,[peer,...aa])),sign(stunMsg(10,connectTid,[peer,...aa]))]);pm=permissionMsg,cm=connectMsg;await cw.write(cat(am,pm,cm));r=await readControl(allocateTid)}else if(r.type===259){permissionTid=newTid(),connectTid=newTid();[pm,cm]=await Promise.all([sign(stunMsg(8,permissionTid,[peer,...aa])),sign(stunMsg(10,connectTid,[peer,...aa]))]);await cw.write(cat(pm,cm))}else{throw 0}if(r?.type!==259)throw 0;r=await readControl(permissionTid);if(r?.type!==264)throw 0;r=await readControl(connectTid);if(r?.type!==266||!r.attrs[42])throw 0;await dataPromise;const dw=data.writable.getWriter(),dr=data.readable.getReader();tid=newTid();await dw.write(await sign(stunMsg(11,tid,[stunAttr(42,r.attrs[42]),...aa])));let extra;[r,extra]=await readMatching(dr,tid);if(r?.type!==267)throw 0;dr.releaseLock(),dw.releaseLock();return{readable:data.readable,writable:data.writable,close:close,extra:extra}}catch{close();return null}};const parseProtocolChunk=chunk=>{const len=chunk.length;const result={success:false,needMore:false,handshake:null,parsedRequest:null};let isVL=false;if(len>=17){isVL=true;for(let i=0;i<16;i++){if(chunk[i+1]!==uuidBytes[i]){isVL=false;break}}}if(isVL){if(len<18)return result.needMore=true,result;const offset=19+chunk[17];if(len<offset+4)return result.needMore=true,result;let addrType=chunk[offset+2];if(addrType!==1)addrType+=1;const addrLen=addrType===3?offset+3<len?chunk[offset+3]:null:addrType===1?4:addrType===4?16:-1;if(addrLen===null)return result.needMore=true,result;if(addrLen>0){const addrOffset=addrType===3?offset+4:offset+3;const dataOffset=addrOffset+addrLen;if(len<dataOffset)return result.needMore=true,result;const port=chunk[offset]<<8|chunk[offset+1];result.handshake=new Uint8Array([chunk[0],0]);result.success=true;result.parsedRequest={addrType:addrType,addrBytes:chunk.subarray(addrOffset,addrOffset+addrLen),dataOffset:dataOffset,port:port,isDns:port===53};return result}}if(len>=56){let isTJ=true;for(let i=0;i<56;i++){if(chunk[i]!==hashBytes[i]){isTJ=false;break}}if(isTJ){if(len<60)return result.needMore=true,result;const addrType=chunk[59];const addrLen=addrType===3?60<len?chunk[60]:null:addrType===1?4:addrType===4?16:-1;if(addrLen===null)return result.needMore=true,result;if(addrLen>0){const addrOffset=addrType===3?61:60;const dataOffset=addrOffset+addrLen+4;if(len<dataOffset)return result.needMore=true,result;const portOffset=addrOffset+addrLen;const port=chunk[portOffset]<<8|chunk[portOffset+1];result.success=true;result.parsedRequest={addrType:addrType,addrBytes:chunk.subarray(addrOffset,addrOffset+addrLen),dataOffset:dataOffset,port:port,isDns:port===53};return result}}}return len<56?(result.needMore=true,result):result};const dohJsonOptions={headers:{Accept:"application/dns-json"}},dohHeaders={"content-type":"application/dns-message"};const dohDnsResolve=async(hostname,recordType)=>{try{const response=await fetch(`${dohEndpoint}?name=${encodeURIComponent(hostname)}&type=${recordType}`,dohJsonOptions);if(!response.ok)return null;const dnsResult=await response.json();const answer=dnsResult.Answer||dnsResult.answer;if(!answer||answer.length===0)return null;return answer}catch{return null}};const dnsAResult=async hostname=>{const answer=await dohDnsResolve(hostname,"A");if(!answer)return null;let ip=null;for(let i=0,len=answer.length;i<len;i++)if(answer[i].type===1&&answer[i].data){ip=answer[i].data;break}return ip};const dohDnsHandler=async payload=>{if(payload.byteLength<2)return null;const dnsQueryData=payload.subarray(2);let dnsQueryResult;try{const resp=await fetch(dohEndpoint,{method:"POST",headers:dohHeaders,body:dnsQueryData});if(!resp.ok)return null;dnsQueryResult=await resp.arrayBuffer()}catch{return null}const udpSize=dnsQueryResult.byteLength;const packet=new Uint8Array(2+udpSize);packet[0]=udpSize>>8&255,packet[1]=udpSize&255;packet.set(new Uint8Array(dnsQueryResult),2);return packet};const txtdnsResult=async txtdns=>{const answer=await dohDnsResolve(txtdns,"TXT");if(!answer)return null;let txtData,i=0,len=answer.length;for(;i<len;i++)if(answer[i].type===16){txtData=answer[i].data;break}if(!txtData)return null;if(txtData.charCodeAt(0)===34&&txtData.charCodeAt(txtData.length-1)===34)txtData=txtData.slice(1,-1);const raw=txtData.split(/,|\\010|\n/),prefixes=[];for(i=0,len=raw.length;i<len;i++){const s=raw[i].trim();if(s)prefixes.push(s)}return prefixes.length?prefixes:null};const proxyIpRegex=/william|fxpip|hhtxt/;const connectProxyIp=async(param,txt)=>{if(txt||proxyIpRegex.test(param)){const resolvedIps=await txtdnsResult(param);if(!resolvedIps||resolvedIps.length===0)return null;const[host,port]=parseHostPort(resolvedIps[Math.random()*resolvedIps.length|0],443);return createConnect(host,port)}const[host,port]=parseHostPort(param,443);return createConnect(host,port)};const strategyExecutorMap=new Map([[0,async({addrType:addrType,port:port,addrBytes:addrBytes})=>{const hostname=binaryAddrToString(addrType,addrBytes);return createConnect(hostname,port)}],[1,async({addrType:addrType,port:port,addrBytes:addrBytes},param)=>connectViaSocksProxy(addrType,port,param,addrBytes)],[2,async({addrType:addrType,port:port,addrBytes:addrBytes},param)=>connectViaHttpProxy(addrType,port,param,addrBytes)],[6,async({addrType:addrType,port:port,addrBytes:addrBytes},param)=>connectViaHttpProxy(addrType,port,param,addrBytes,true)],[5,async(parsedRequest,param)=>connectViaTurnProxy(param,parsedRequest)],[7,async(parsedRequest,param)=>connectViaTurnProxy(param,parsedRequest,true)],[3,async(_parsedRequest,param,txt)=>connectProxyIp(param,txt)]]);const paramRegex=/(speed|gs5|s5all|ghttp|httpall|ghttps|httpsall|gturn|turnall|gturns|turnsall|s5|socks|http|https|turn|turns|txtip|ip)(?:=|:\/\/|%3A%2F%2F)([^&]+)|(proxyall|globalproxy|global)/gi;const urlListCacheDict=new Map,urlListCacheKeys=new Array(urlParamCacheLimit);let urlListCacheIndex=0;const establishTcpConnection=async(parsedRequest,request)=>{let u=request.url,clean=u.slice(u.indexOf("/",10)+1),l=clean.length,list=[],speed;const c=clean.charCodeAt(l-1);if(c===47||c===61)clean=clean.slice(0,l-1);const cachedResult=urlListCacheDict.get(clean);if(cachedResult!==undefined){list=cachedResult.list,speed=cachedResult.speed}else{if(clean.length<6){list.push({type:0},{type:3,param:coloToProxyMap.get(request.cf?.colo)??proxyIpAddrs.US},{type:3,param:finallyProxyHost})}else{const p=Object.create(null);paramRegex.lastIndex=0;let m;while(m=paramRegex.exec(clean)){p[(m[1]||m[3]).toLowerCase()]=m[2]?m[2].charCodeAt(m[2].length-1)===61?m[2].slice(0,-1):m[2]:true}if(p.speed)speed=p.speed;const s5=p.gs5||p.s5all||p.s5||p.socks,http=p.ghttp||p.httpall||p.http,https=p.ghttps||p.httpsall||p.https,turn=p.gturn||p.turnall||p.turn,turns=p.gturns||p.turnsall||p.turns;const proxyAll=!!(p.gs5||p.s5all||p.ghttp||p.httpall||p.ghttps||p.httpsall||p.gturn||p.turnall||p.gturns||p.turnsall||p.proxyall||p.globalproxy||p.global);if(!proxyAll)list.push({type:0});const add=(v,t,txt)=>{if(!v)return;const parts=decodeURIComponent(v).split(",").filter(Boolean);for(let i=0;i<parts.length;i++)list.push(txt?{type:t,param:parts[i],txt:txt}:{type:t,param:t===1||t===2||t===5||t===6||t===7?parseAuthString(parts[i]):parts[i]})};for(let i=0;i<proxyStrategyOrder.length;i++){const k=proxyStrategyOrder[i];add(k==="socks"?s5:k==="http"?http:k==="https"?https:k==="turn"?turn:turns,k==="socks"?1:k==="http"?2:k==="https"?6:k==="turn"?5:7)}if(proxyAll){if(!list.length)list.push({type:0})}else{add(p.ip,3),add(p.txtip,3,true);list.push({type:3,param:coloToProxyMap.get(request.cf?.colo)??proxyIpAddrs.US},{type:3,param:finallyProxyHost})}}const oldKey=urlListCacheKeys[urlListCacheIndex];if(oldKey!==undefined)urlListCacheDict.delete(oldKey);urlListCacheKeys[urlListCacheIndex]=clean;urlListCacheDict.set(clean,{list:list,speed:speed});urlListCacheIndex=(urlListCacheIndex+1)%urlParamCacheLimit}for(let i=0;i<list.length;i++){try{const exec=strategyExecutorMap.get(list[i].type);const socket=await(exec?.(parsedRequest,list[i].param,list[i].txt));if(socket)return{socket:socket,speed:speed}}catch{}}return null};const manualPipe=async(readable,writable,close,speed)=>{const n=parseFloat(speed),speedLimit=n>0;let pipeBufferSize=bufferSize,pipeFlushTime=flushTime,pipeStartThreshold=startThreshold;if(speedLimit){pipeStartThreshold=n>256?Number.MAX_SAFE_INTEGER:n*1048576;let bestSize=pipeBufferSize,bestTime=Infinity,bestDiff=Infinity;for(let size=262144;size<=524288;size+=65536){const timeMs=Math.max(2,Math.round(size*1e3/pipeStartThreshold)),diff=Math.abs(size*1e3/timeMs-pipeStartThreshold);if(diff<bestDiff||diff===bestDiff&&timeMs<bestTime)bestSize=size,bestTime=timeMs,bestDiff=diff}pipeBufferSize=bestSize,pipeFlushTime=bestTime}const safeBufferSize=pipeBufferSize-maxChunkLen,fastFlushOffset=maxChunkLen<<1;let bufferView=new Uint8Array(pipeBufferSize),spareBuffer=new ArrayBuffer(maxChunkLen);let offset=0,totalBytes=0,time=0,timerId=null,resume=null,isReading=false,needsFlush=false,protectFlush=false;let fastFlush=true;const flushBuffer=()=>{if(isReading)return needsFlush=true;fastFlush=offset<fastFlushOffset;if(offset>0){offset>safeBufferSize?(writable.send(bufferView.subarray(0,offset)),bufferView=new Uint8Array(pipeBufferSize)):writable.send(bufferView.slice(0,offset));offset=0}needsFlush=false,protectFlush=false,timerId&&(clearTimeout(timerId),timerId=null),resume?.(),resume=null};const reader=readable.getReader({mode:"byob"});try{while(true){const useSpare=offset>0&&protectFlush;let readBuffer=bufferView.buffer,readOffset=offset;isReading=offset>0;useSpare&&(readBuffer=spareBuffer,readOffset=0,isReading=false);const{done:done,value:value}=await reader.read(new Uint8Array(readBuffer,readOffset,maxChunkLen));isReading=false;useSpare?(bufferView.set(value,offset),spareBuffer=value.buffer):bufferView=new Uint8Array(value.buffer);if(done)break;const chunkLen=value.byteLength;if(!chunkLen){needsFlush&&flushBuffer();continue}offset+=chunkLen,totalBytes+=chunkLen;if(needsFlush||chunkLen<2048){flushBuffer()}else{if(fastFlush||chunkLen<28672){if(!speedLimit)totalBytes=0;time=2}else if(totalBytes>pipeStartThreshold)time=pipeFlushTime;timerId||=setTimeout(flushBuffer,time),protectFlush=chunkLen<maxChunkLen;offset>safeBufferSize&&(totalBytes>pipeStartThreshold?await new Promise(r=>resume=r):flushBuffer())}}}catch{offset=0,close?.()}finally{isReading=false,flushBuffer()}};const createAsyncMicrotaskQueue=(consume,close)=>{const queue=new Array(2048);let head=0,tail=0,size=0,coalesceBuffer=null,drainActive=false,closed=false;const closeQueue=()=>{if(closed)return;closed=true;for(let i=0;i<2048;i++)queue[i]=null;close?.()};const shift=()=>{const chunk=queue[head];queue[head]=null,head=head+1&2047,size--;return chunk};const drainQueue=async()=>{if(closed)return;try{while(size>0&&!closed){if(!enqueue.writer){await consume(shift());continue}let chunk=queue[head];if(chunk.byteLength>=maxChunkLen){await enqueue.writer.write(shift());continue}let mergedLength=0;coalesceBuffer||=new Uint8Array(maxChunkLen);while(size>0&&mergedLength+queue[head].byteLength<=maxChunkLen){chunk=shift(),coalesceBuffer.set(chunk,mergedLength),mergedLength+=chunk.byteLength}if(mergedLength>0)await enqueue.writer.write(coalesceBuffer.subarray(0,mergedLength))}}catch{closeQueue()}finally{drainActive=false}};const enqueue=chunk=>{if(closed)return;chunk=chunk.constructor===Uint8Array?chunk:new Uint8Array(chunk);if(enqueue.writer&&!chunk.byteLength)return;if(size===2048)return closeQueue();queue[tail]=chunk,tail=tail+1&2047,size++;if(!drainActive)drainActive=true,queueMicrotask(drainQueue)};return enqueue};const handleSession=async(chunk,state,request,writable,close,isEarlyData=false)=>{state.needMore=false;const parsed=parseProtocolChunk(chunk);if(parsed.handshake)writable.send(parsed.handshake);if(!parsed.success)return parsed.needMore?state.needMore=true:close();const parsedRequest=parsed.parsedRequest;const payload=chunk.subarray(parsedRequest.dataOffset);if(parsedRequest.isDns){const dnsPack=await dohDnsHandler(payload);if(dnsPack?.byteLength)writable.send(dnsPack);if(!isEarlyData)return close()}else{const tcpResult=await establishTcpConnection(parsedRequest,request);if(!tcpResult)return close();state.tcpSocket=tcpResult.socket;const tcpWriter=state.tcpSocket.writable.getWriter();if(payload.byteLength)await tcpWriter.write(payload);if(state.xwebPipeTo)return tcpWriter.releaseLock();state.tcpWriter||=createAsyncMicrotaskQueue(null,close);state.tcpWriter.writer=tcpWriter;if(state.tcpSocket.extra?.length)await writable.send(state.tcpSocket.extra);if(state.xwebPipeTo)return;manualPipe(state.tcpSocket.readable,writable,close,tcpResult.speed)}};const handleWebSocketConn=async(webSocket,request)=>{const refererHeader=request.headers.get("Referer");const protocolHeader=refererHeader||request.headers.get("sec-websocket-protocol");let earlyDataHeader=null;if(refererHeader){earlyDataHeader=protocolHeader.slice(request.headers.get("host").length)}else if(protocolHeader){earlyDataHeader=protocolHeader}const earlyData=earlyDataHeader?Uint8Array.fromBase64(earlyDataHeader,{alphabet:"base64url"}):null;const state={tcpWriter:null,tcpSocket:null};const close=()=>{try{state.tcpSocket?.close()}catch{}try{webSocket.close(1011,"WebSocket is closed")}catch{}};const processingQueue=createAsyncMicrotaskQueue(chunk=>handleSession(chunk,state,request,webSocket,close,earlyData!==null),close);state.tcpWriter=processingQueue;if(earlyData)processingQueue(earlyData);webSocket.addEventListener("message",event=>processingQueue(event.data));webSocket.addEventListener("error",close);webSocket.addEventListener("close",close)};const xwebHeaders={"Content-Type":"application/octet-stream","grpc-status":"0","X-Accel-Buffering":"no","Cache-Control":"no-store"};const handleXwebPost=async request=>{const reader=request.body?.getReader({mode:"byob"});if(!reader)return new Response(null,{status:400});const state={tcpWriter:null,tcpSocket:null,needMore:false,xwebPipeTo:true};const bridge=new IdentityTransformStream,responseWriter=bridge.writable.getWriter();let xwebBuffer=new ArrayBuffer(8192),used=0;const close=()=>{state.tcpSocket?.close()};const writable={send(chunk){if(chunk?.byteLength)return responseWriter.write(chunk)}};(async()=>{while(true){if(used>0){const payload=new Uint8Array(xwebBuffer,0,used);state.tcpWriter?await state.tcpWriter(payload.slice()):(state.needMore=false,await handleSession(payload,state,request,writable,close));if(state.tcpSocket&&state.xwebPipeTo){state.xwebPipeTo=false;responseWriter.releaseLock();state.tcpSocket.readable.pipeTo(bridge.writable).catch(close)}if(!state.needMore){used=0;continue}}const{done:done,value:value}=await reader.read(new Uint8Array(xwebBuffer,used,used===0?8192:4096));if(done)return close();xwebBuffer=value.buffer;used+=value.byteLength}})().catch(close);return new Response(bridge.readable,{headers:xwebHeaders})};export default{async fetch(request){if(request.method==="POST"&&request.headers.get("content-type")==="application/grpc-web")return handleXwebPost(request);if(request.headers.get("Upgrade")==="websocket"){const{0:clientSocket,1:webSocket}=new WebSocketPair;webSocket.accept({allowHalfOpen:true}),webSocket.binaryType="arraybuffer";handleWebSocketConn(webSocket,request);return new Response(null,{status:101,webSocket:clientSocket})}return new Response(html,{status:200,headers:{"Content-Type":"text/html; charset=UTF-8"}})}};